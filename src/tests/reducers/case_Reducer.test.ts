import { caseReducer, initialState } from '../../store/reducers/case_Reducer'

describe('case reducer', () => {
  it('should be defined', () => {
    expect(caseReducer).toBeDefined()
  })

  it('should has initialState', () => {})
})

export {}
